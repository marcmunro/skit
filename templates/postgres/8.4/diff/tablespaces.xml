<?xml version="1.0" encoding="UTF-8"?>

<skit:inclusion
   xmlns:skit="http://www.bloodnok.com/xml/skit"
   xmlns:xi="http://www.w3.org/2003/XInclude">

  <dbobject type="tablespace" key="name">
    <attribute name="owner">
<!--
    This is left in place as an example.  It is not clear that allowing
    this is a good idea as it can easily lead to dependency cycles.
    Maybe the code that supports this should be eliminated.

      <dependency-set 
	  priority="100"
	  eval_example="{eval.(username)}"
	  fallback="grant.role.{old.owner}.{new.owner}"
	  parent="//dbobject[database]">
	<dependency fqn="privilege.role.{old.owner}.superuser"/>
	<dependency pqn="grant.role.{old.owner}.{new.owner}"/>
      </dependency-set>
-->

    </attribute>
    <attribute name="location"/>
    <element type="comment">
      <text/>
    </element>
  </dbobject>	

</skit:inclusion>  
